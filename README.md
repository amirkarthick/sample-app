# sample-app

This repository contains the CI/CD lab sample application for GKE + Cloud Build.

Project ID: qwiklabs-gcp-03-52cd18060510

Placeholders:
- Replace `<your-region>` and `<your-zone>` in `cloudbuild.yaml`, `cloudbuild-dev.yaml`, and the deployment YAMLs before running in Cloud Shell.

See parent README for full lab instructions.
